#pragma once




namespace traffic
{
    /**
     * SPOT SPEED 
     * obtained for limit (t2-t1) -> 0
     * @brief
     * spot speed = dx / dt
     */
    double spot_speed();
    // double spot_speed(vector< unordered_multiset<pair<double, double>, pair<double, double> > >&);
} // namespace traffic
